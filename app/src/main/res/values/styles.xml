<resources>

    <!-- Base application theme. -->
    <style name="MaterialTheme" parent="ParentMaterialTheme" />
    <!-- Customize your theme here. -->
    <style name="ParentMaterialTheme" parent="Theme.AppCompat.Light">
        <!-- Parent theme for all Pre-Lolipop. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlHighlight">@color/colorHighlignt</item>
        <item name="colorButtonNormal">@color/colorButtonNormal</item>
        <item name="colorControlActivated">@color/coclorControlAcitivate</item>
        
    </style>

</resources>
